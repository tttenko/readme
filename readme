/**
 * Проверяет, что метод плановой очистки (scheduledClearAllCaches)
 * вызывает фактическую очистку всех кэшей.
 * Тест не зависит от cron и таймзоны.
 */
@Test
void shouldInvokeScheduledClearAllCaches() {
    // given
    Cache cache = cacheManager.getCache("users");
    cache.put("123", "scheduled-test");
    assertThat(cache.get("123").get()).isEqualTo("scheduled-test");

    // when
    scheduler.scheduledClearAllCaches();

    // then
    assertThat(cache.get("123")).isNull();
}
