/**
 * Демонстрационный сервис, использующий Spring Cache.
 * Показывает базовую работу @Cacheable с ключом по идентификатору.
 */
@Slf4j
@Service
public class TestCacheService {

  /**
   * Возвращает «псевдо-пользователя» по id.
   * Первый вызов — вычисление и логирование; повторные — из кэша "users".
   *
   * @param id идентификатор пользователя
   * @return строка с префиксом "User-<id>"
   */
  @Cacheable(cacheNames = "users", key = "#id", unless = "#result == null")
  public String getUserById(String id) {
    log.info("Loading user {}...", id);
    return "User-" + id;
  }
}
