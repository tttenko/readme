/**
 * Тестовая конфигурация, переопределяющая CacheProps для интеграционных тестов.
 * Делает значения (имена кэшей/TTL/CRON) стабильными и независимыми от prod-конфига.
 */
@TestConfiguration
public class SchedulerConfig {

  /**
   * Тестовые свойства кэша с аннотацией @Primary,
   * чтобы перекрыть продовую конфигурацию.
   *
   * @return тестовый CacheProps
   */
  @Bean
  @Primary
  public CacheProps cacheProps() {
    return new CacheProps(List.of("users"), 5, "0 0 3 * * *", "Europe/Moscow");
  }
}
