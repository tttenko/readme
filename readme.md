```java

/**
 * Отправляет POST-запрос в МД для получения данных по критериям.
 *
 * @param request объект запроса с фильтрами и параметрами
 * @param context контекст поиска (например, BOOK или PRICE)
 * @return ответ МД в виде {@link GetItemsSearchResponse}
 * @throws MdaServerErrorException если не удалось сериализовать запрос или выполнить вызов
 */

```
