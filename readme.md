```java

/**
 * Юнит-тесты для {@link CurrencyCacheOps}.
 * Проверяется корректность загрузки валют из МД, вызовы backend-сервиса
 * и применение маппера. Кеширование в этом классе не используется.
 */

 /**
 * Юнит-тесты для {@link CurrencyService2}.
 * Проверяется выбор логики:
 * загрузка всех валют через {@link CurrencyCacheOps} или
 * выборочная загрузка через {@link CacheGetOrLoadService}.
 */

 /**
 * Юнит-тесты для {@link LoaderCurrencyByCode}.
 * Проверяется работа BatchLoader-контракта и корректная загрузка валют
 * по списку кодов через {@link BaseMasterDataRequestService}.
 */

```
