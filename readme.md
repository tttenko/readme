```java

/**
 * Набор unit-тестов для {@link LoaderNdsByRate}.

 * <p>Проверяет построение запроса к {@link BaseMasterDataRequestService}
 * и корректное формирование бакетов {@link NdsService2.RateBucket} по ключам кеша.
 *
 * @see LoaderNdsByRate
 */

/**
 * Набор unit-тестов для {@link NdsService2}.

 * <p>Проверяет формирование ключей кеша и применение фильтров по ставкам и кодам
 * при получении базовых ставок НДС через {@link CacheGetOrLoadService}.
 *
 * @see NdsService2
 */
```
