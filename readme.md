```java
/**
 * Приводит ноду items из ответа МД к плоской Map<String, String>.
 * Если элемент имеет вид {"item": {...}, "values": [...]},
 * возвращаем внутренний item. Для старых справочников без обёртки
 * возвращаем сам node. Таким образом, DataMapper всегда получает
 * плоскую мапу полей сущности.
 */

```
