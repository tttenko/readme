```java/**
Для этого select лучше всего отработает ix_exchange_rate_lookup (isonum1, isonum2, use_date desc) — он у нас как раз есть, поэтому план будет индексный.

UNIQUE (fxratesubtype, isonum1, isonum2, use_date) нужен не для чтения, а чтобы гарантировать уникальность и корректный upsert по бизнес-ключу.

fxratesubtype — это FXRates@FXRateSubType из XML (например FXCB). Он в уникальном ключе нужен, если допускаются разные subtype для одной пары валют/даты; если subtype всегда один и тот же, тогда его можно убрать, но сейчас он оставлен намеренно под контракт входных данных.
```
