```java
Если в ноде есть item, то в этом пути обработки мы берем его как единственный источник данных, а остальные поля игнорируем. атрибуты не нужны
А если нужны атрибуты, то используется метод mapSubItems, там они не выбрасываются

```
