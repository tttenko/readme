```java

/**
 * Тестовый срез для {@link TerBankCacheOps} с использованием Spring Cache (Caffeine).
 *
 * Цели проверки:
 * <ul>
 *   <li>Корректная работа кеширования: первый вызов идёт во внешний сервис, последующие — хиты кеша.</li>
 *   <li>Имена/ключи кешей: {@code TerBankService2.TB_ALL} и {@code TerBankService2.TB_REQ_ALL}
 *       с ключом {@code "ALL"}.</li>
 *   <li>Тип значения, сохранённого под ключом (raw value) соответствует ожидаемому DTO-списку.</li>
 *   <li>Изоляция кешей: TB_ALL и TB_REQ_ALL не мешают друг другу.</li>
 *   <li>Конкурентный доступ при {@code sync = true}: при множественных параллельных вызовах
 *       внешний сервис вызывается один раз.</li>
 *   <li>Верификация того, какой метод backend-а вызывается:
 *       {@code requestData(...)} для «без атрибутов» и
 *       {@code requestDataWithAttribute(...)} для «с атрибутами».</li>
 * </ul>
 *
 * Контекст теста минимальный: поднимается конфигурация кеша {@code TestCacheConfig} и SUT {@code TerBankCacheOps},
 * остальные зависимости подменяются через {@code @MockitoBean}.
 */

/**
 * Сбрасывает состояние кешей и настраивает стабы внешних зависимостей
 * (успешные ответы backend + мапперы).
 */
```
