```java
если completedDate != null → Завершён
иначе если этап раньше текущего витринного статуса → Завершён
иначе если этап равен текущему статусу → В работе
иначе → План

Причём проверка completedDate != null имеет наивысший приоритет. То есть даже если по позиции этап формально был бы План или В работе, наличие completedDate сразу делает его Завершён.

```
