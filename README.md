```java

evaluationOrder — это технический порядок выполнения стратегий для оптимизации расчёта, а priority из application.yaml — порядок отображения отклонений на UI. Например, проверка метрик имеет высокий UI-приоритет, но выполняется последней как самая тяжёлая.
```
