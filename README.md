```java
Получены финальные ответы человека. Обнови change proposal initiative-export-portfolio, не выполняя apply.

В выгрузку входят все инициативы, включая disabled.
Добавляется новый столбец is_disable типа boolean:
false — активная инициатива;
true — disabled-инициатива.
В результате файл содержит 27 столбцов: 17 существующих + 10 новых. Обнови это количество во всех FR, AC, What Changes и остальных местах proposal.
Порядок строк отдельно не регламентируется. Сохраняется порядок, используемый существующей admin-выгрузкой.
Email бизнес-владельца, IT-владельца и контактных лиц записываются в одну ячейку через ;. Порядок email не имеет значения. Требование дедупликации отсутствует — не добавляй его самостоятельно.
Для каждого этапа дедлайн берётся из statusSla.plannedDate.
Жизненный статус этапа определяется в следующем приоритетном порядке:
completedDate != null → Завершён;
иначе этап расположен раньше текущего витринного статуса инициативы → Завершён;
иначе этап равен текущему витринному статусу → В работе;
иначе этап расположен после текущего витринного статуса → План.
Жёсткого ограничения времени формирования файла нет. Целевой ориентир — желательно сформировать файл в течение 60 секунд. Удали обязательное требование про 30 секунд.
Так как жёсткого timeout нет, не связывай 408 с обязательным превышением 30/60 секунд. Если причина возврата 408 не определена исходной постановкой, оставь отдельно как TBD либо отметь необходимость подтверждения API-контракта.
Ссылка Pixso не требуется — удалить соответствующий TBD.
После изменений обнови Open Questions: удали закрытые вопросы и оставь только действительно нерешённые. Apply к initiative-catalog пока не выполнять.

```
