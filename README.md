```java
Скорректируй план перед выполнением.

Используй имя change proposal initiative-export-portfolio, так как именно оно указано в связанной постановке, если локальные правила репозитория явно не требуют другого имени.
Не объединяй все TBD в NFR-NEW-1. К NFR относится только требование по времени формирования файла (30 секунд, пока TBD/требует подтверждения).
Неопределённости по включению disabled-инициатив, порядку строк, порядку/дедупликации email и алгоритму определения статусов этапов вынеси в Open Questions/TBD соответствующих функциональных требований или business rules.
Проверь, достаточно ли одного FR-NEW-1, или состав выгрузки, статусы этапов и дедлайны должны быть разделены на несколько самостоятельных FR согласно правилам atomic requirements репозитория.
Существующий /api/v1/admin/ai-agent-download должен остаться без изменений.
Пока создаём только change proposal. Целевые approved-документы initiative-catalog не изменять до отдельного apply после human approval.

После корректировки покажи финальный план ещё раз.

```
