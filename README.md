```java
блоки не отображались, потому что экспорт брал их напрямую из ai_agent.block_id, который у агентов был NULL. Исправлено получение блока через связь ai_agent → division → block, также обновлена фильтрация по blockId.
  ```
