```java
без code в таблице и без id в конфигурации связать конфиг со справочником можно только через metrics_directory.name. Поэтому значения code в конфиге должны совпадать с name в БД. Если name изменится, поиск сломается
```
