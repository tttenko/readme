```java
Получено дополнительное подтверждённое требование к change proposal initiative-export-portfolio.

Нужно добавить в XLSX-выгрузку кликабельную ссылку на инициативу в Пульт.

Функциональное требование

Добавить новый столбец:

Ссылка на инициативу в Пульт

Значение формируется по шаблону:

https://pult.sber.ru/ai/initiatives/{id}

где {id} — ID текущей инициативы.

Ячейка Excel должна содержать активную кликабельную гиперссылку, а не только текстовое значение URL.

Существующие первые 17 столбцов admin-выгрузки не менять и не переставлять.

Новый порядок дополнительных столбцов:

is_disable
Ссылка на инициативу в Пульт
Email контакта
Статус этапа — Концепция
Дедлайн этапа — Концепция
Статус этапа — PoC
Дедлайн этапа — PoC
Статус этапа — MVP
Дедлайн этапа — MVP
Статус этапа — Целевое решение
Дедлайн этапа — Целевое решение

Итого файл содержит 28 столбцов: 17 существующих + 11 новых.

Обнови в proposal.md:

What Changes;
FR-NEW-2;
полный список колонок;
Acceptance Criteria для FR-NEW-2;
все остальные места, где сейчас указано 27 столбцов, 10 новых или перечисляется состав выгрузки.

Отдельный FR для ссылки не создавай, если локальные правила atomic requirements допускают оставить её частью требования о структуре Excel (FR-NEW-2).

Техническое решение

Уточни в proposal/tasks требования к будущему изменению technical.md.

При последующем apply в docs/features/initiative-catalog/technical.md должно быть явно описано:

Новый GET /api/v1/ai-agent/initiatives/export формирует XLSX полного реестра для TRANSFORMATION_OFFICE.
Существующий GET /api/v1/admin/ai-agent-download по контракту и поведению не изменяется.
Перед проектированием реализации необходимо изучить существующую цепочку admin-export и максимально переиспользовать механизм формирования первых 17 колонок, не дублируя существующую логику без необходимости.
Новый export получает все инициативы, включая disabled, без применения фильтров, поиска и пагинации каталога.
В новый XLSX добавляется 11 дополнительных колонок, включая is_disable и Ссылка на инициативу в Пульт.
Для Ссылка на инициативу в Пульт URL строится для ID конкретной инициативы по шаблону https://pult.sber.ru/ai/initiatives/{id} и записывается в Excel как hyperlink.
Не хардкодить production URL в backend автоматически, если в проекте существует конфигурация base URL Пульта/frontend. Сначала найти существующую конфигурацию и использовать её. Если подходящей конфигурации нет — в technical.md явно предложить конфигурируемый base URL вместо скрытого hardcode. Не придумывать существующие property/class names.
Для is_disable выгружается boolean:
false — активная инициатива;
true — disabled.
Email контакта содержит email бизнес-владельца, IT-владельца и контактных лиц в одной ячейке через ;; порядок не регламентируется, дедупликация не требуется.
Данные этапов брать из statusSla:
дедлайн — plannedDate;
completedDate != null → Завершён;
иначе этап раньше текущего витринного статуса → Завершён;
иначе этап равен текущему витринному статусу → В работе;
иначе этап после текущего витринного статуса → План.
Для отсутствующего plannedDate Excel-ячейка дедлайна должна быть пустой.
Порядок строк специально не переопределяется — необходимо сохранить поведение существующей admin-выгрузки.
Жёсткого timeout нет; целевой ориентир формирования — до 60 секунд. Не реализовывать искусственный timeout только ради этого ориентира.

В technical.md при apply также должны быть отражены:

Components;
Data flow;
API changes;
Security / TRANSFORMATION_OFFICE;
Performance;
Backward compatibility;
Testing considerations.

В Testing considerations предусмотреть как минимум:

TRANSFORMATION_OFFICE может скачать файл;
другие роли получают 403;
выгрузка содержит active + disabled инициативы;
структура содержит ровно 28 колонок в правильном порядке;
первые 17 колонок сохраняют существующее поведение;
is_disable корректно формируется;
ссылка строится из ID инициативы и является Excel hyperlink;
несколько email объединяются через ;;
статусы всех четырёх этапов рассчитываются по заданному правилу;
plannedDate попадает в deadline;
отсутствующий plannedDate даёт пустую ячейку;
существующий /api/v1/admin/ai-agent-download не получает регрессию.

Сейчас apply не выполнять и approved-документы feature не изменять. Обновить только change proposal/tasks так, чтобы после human approval technical.md был применён по этим правилам.

```
