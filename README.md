```java

ResponseFactory подходит для обертки ответа в ResponseEntity, а мой helper нужен для единообразной сборки ResultObj. Поэтому оставил его, чтобы не дублировать наполнение messages/data/count в каждом контроллере.
```
